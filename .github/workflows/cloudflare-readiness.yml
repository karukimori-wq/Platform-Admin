name: Cloudflare Migration Readiness

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - infra/cloudflare-clerk-migration

permissions:
  contents: read

jobs:
  readiness:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Typecheck Next.js application
        run: npm run typecheck

      - name: Next.js build
        run: npm run build

      - name: Validate Wrangler config
        run: npm run cf:check

      - name: Generate Cloudflare binding types
        run: npm run cf-typegen

      # Do not rerun the browser/Next tsconfig after emitting Workers runtime
      # globals into cloudflare-env.d.ts. Workers intentionally types JSON as
      # unknown, which changes DOM Response.json globally. The application is
      # typechecked before generation; OpenNext/Wrangler dry-run validates the
      # Worker bundle below.

      - name: Apply D1 schema locally
        run: npm run d1:schema:local

      - name: Verify D1 schema locally
        run: npm run d1:verify:local

      - name: Cloudflare deployment dry run
        run: npm run deploy:dry-run
