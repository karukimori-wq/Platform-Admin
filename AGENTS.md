# Platform Admin Agent Instructions

Before substantial monitoring, ingestion, observability, deployment, cross-app integration, or production-readiness work, consult `karukimori-wq/ai-development-intelligence` using its task retrieval protocol, ranking/index, and `memory/platform-admin/`.

## Operational learning

For substantial tasks, follow `core/task-run-standard.md` and `core/operational-learning-loop.md`. Use a non-sensitive `taskRunId` when event capture is available. Verify current app endpoints/contracts before applying remembered monitoring guidance. Use the strict failure-outcome attribution policy for any prevention or recurrence metric.

## Authority

Current Platform Admin code is authoritative for implementation. `professional-platform-contracts` is authoritative for formal contracts. Development intelligence is advisory and must not become a product source of truth.

## After meaningful work

Evaluate extraction and capture only reusable failures, decisions, patterns, contradictions, or stable evidence. Prefer updating existing knowledge over duplicates.

Store operational lessons, not secret values, tokens, credentials, raw sensitive logs, or personal data.